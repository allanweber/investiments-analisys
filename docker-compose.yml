services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: investiments
    ports:
      - '5432:5432'
    volumes:
      - investiments_pg:/var/lib/postgresql/data

volumes:
  investiments_pg:
